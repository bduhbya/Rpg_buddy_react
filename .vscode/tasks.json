{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run Main App",
      "type": "shell",
      "command": "npm run dev",
      "runOptions": {
        "runOn": "default"
      },
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
